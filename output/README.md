# Output files

`runs/<run-id>/` is the maintained program's output boundary. Each run owns its
manifest and generated files. `legacy/` contains the previous extraction and
conversion results preserved during consolidation; `work/` contains intermediate
or validation material. These binary corpora remain local and are ignored by Git.
