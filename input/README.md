# Input files

`raw/` contains the physically consolidated APK, OBB, NPK, device-copy, and
other source files. Treat it as read-only. `manifests/` stores provenance and
hash records; `profiles/` stores explicit format profiles. Raw inputs are local
and ignored by Git.
