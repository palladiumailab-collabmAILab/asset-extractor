# Evidence and historical records

`history/` contains the curated records used for planning and review.
`raw/` contains the complete physically consolidated reports and logs, which are
kept locally but ignored by Git because some are large generated inventories.
`HISTORY.md` explains the source-to-target classification.
