# Historical programs

These scripts are preserved for provenance and comparison. New automation must
use `programs/src/asset_extractor` and should adopt legacy behavior only after a
fixture and a manifest contract make it reproducible.
